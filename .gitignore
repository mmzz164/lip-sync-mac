# Python
__pycache__/
*.pyc
*.pyo

# This repo only holds the driver scripts. Everything below is generated or
# fetched at setup time (see README.md) and should never be committed:
ComfyUI/
custom_nodes/
venv/
models/
input/
output/
narration/
share/
*.mp4
*.wav
*.png
*.jpg
*.jpeg

# ...except the demo clip and the two inputs it was made from, so the example
# in README "Demo" is runnable as-is. Both inputs are freely licensed; see the
# attribution there before swapping in your own.
!assets/demo.mp4
!assets/demo_portrait.png
!assets/demo_ref.wav

# OS
.DS_Store
